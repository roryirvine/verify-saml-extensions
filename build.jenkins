node() {
  stage("Checkout") {
    checkout scm
  }
  stage("Build Container") {
    def container = docker.image 'verify-saml-extensions'
    container.build()
  }
  stage("Run Container") {
    container = docker.image 'verify-saml-extensions'
    container.Run()
  }
}
