node() {
  stage("Checkout") {
    checkout scm
  }
  stage("Build Container") {
    docker.image('verify-saml-extensions').build()
  }
  stage("Run Container") {
    def container = docker.image('verify-saml-extensions').run()
  }
}
